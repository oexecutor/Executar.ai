# Exemplo: executive-report

Demonstra o mesmo conteúdo estruturado (`content.json`) adaptado a formatos distintos,
com registro de transformações (`adaptation.md`) e o par de dados estruturados
(`output.structured.json`) exigido por FR-005/AI-001.

Os blocos validam contra `../../contracts/content-model.schema.json` (representações e
prioridade obrigatórias). Perfis e orçamentos: `../../contracts/format-profiles.yaml` e
`content-budgets.yaml`. Status: PROVISIONAL onde o formato não tem amostra real (LAC-009).
