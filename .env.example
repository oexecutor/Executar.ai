# EXECUTA.AI environment contract
# Copy to .env.local for local development. Never commit real credentials.

# Public application URL. Configure separately for Preview and Production.
PUBLIC_BASE_URL=https://your-project.vercel.app

# Private server secret used to sign MCP JWTs. Minimum: 32 random bytes.
MCP_JWT_SECRET=replace-with-at-least-32-random-bytes

# Public Supabase configuration. The publishable key is intentionally safe
# for the browser; authorization is enforced by Auth + RLS.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_PUBLISHABLE_KEY=

# Server-only key used by the MCP after the user explicitly authorizes a
# workspace. Never prefix this variable with VITE_ or expose it to the web.
SUPABASE_SERVICE_ROLE_KEY=

# AI plan generation. Vercel deployments use VERCEL_OIDC_TOKEN automatically.
# Set AI_GATEWAY_API_KEY only for local development or a non-Vercel runtime.
AI_GATEWAY_API_KEY=
EXECUTA_AI_MODEL=openai/gpt-5.5

# Existing OAuth client registry remains in Postgres during the incremental
# migration. This may point to the Supabase transaction pooler.
DATABASE_URL=

# E2 — GitHub + Vercel Preview. The two tokens are mandatory and server-only.
# The four non-secret identifiers below are optional overrides; production uses
# the canonical EXECUTA.AI repository, main branch, Vercel project and team.
# The GitHub token needs Contents:write and Pull requests:write only for the
# configured repository. Never expose either token to VITE_ variables.
EDITORIAL_GITHUB_TOKEN=
# Optional server-only aliases: GITHUB_TOKEN or GH_TOKEN.
EDITORIAL_GITHUB_REPOSITORY=oexecutor/P1.Executar.ai
EDITORIAL_GITHUB_BASE_BRANCH=main
EDITORIAL_VERCEL_TOKEN=
# Optional server-only alias: VERCEL_TOKEN.
EDITORIAL_VERCEL_PROJECT_ID=
EDITORIAL_VERCEL_TEAM_ID=

# Optional compatibility variable. Keep empty unless a legacy deployment
# explicitly requires the deprecated operator login.
ADMIN_PASSWORD=

# Opt-in only (DEC-001, 2026-07-28). When "true", requests without a real
# session share a single public workspace instead of getting 401. Real
# authentication is enforced by default; only set this for an
# owner-operated demo deployment that holds no private data. Never set it
# on a deployment serving real users.
ALLOW_PUBLIC_WORKSPACE_FALLBACK=

# Smoke tests. Override in CI, Preview or Production as appropriate.
SMOKE_BASE_URL=http://localhost:3000
