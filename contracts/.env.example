# Existing application
PUBLIC_BASE_URL=

# Existing secrets — production values must remain in Netlify
MCP_JWT_SECRET=
ADMIN_PASSWORD=

# Clarify or remove after source inspection
OBSIDIAN_INTEGRATION_VALUE=

# New application settings
DESK_OS_SCHEMA_VERSION=1.0.0
DESK_OS_STORAGE_ADAPTER=vault
DESK_OS_DEFAULT_WORKSPACE_ID=
DESK_OS_MAX_STEPS_PER_TASK=3
DESK_OS_MAX_ACTIVE_WORKFLOWS=1

# Optional only if the existing storage implementation uses Netlify Blobs
# NETLIFY_BLOBS_CONTEXT is normally provided by Netlify runtime.

# Future, not MVP unless explicitly approved
AI_PROVIDER=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
